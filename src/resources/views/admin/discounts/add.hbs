<div class="container mt-4 admin-discount-add">
  <h2>➕ Thêm quy tắc giảm giá</h2>
  <form method="POST" action="/admin/discounts/add" class="mt-3">
    <div class="mb-3">
      <label>Tổng tối thiểu (₫)</label>
      <input type="number" name="minTotal" class="form-control" required>
    </div>
    <div class="mb-3">
      <label>Phần trăm giảm (%)</label>
      <input type="number" name="discountPercent" class="form-control" required>
    </div>
     <div class="mb-3 form-check">
      <input type="checkbox" name="active" id="activeCheckbox" checked>
      <label for="activeCheckbox">Kích hoạt?</label>
    </div>
    <button type="submit" class="btn btn-success">Lưu</button>
    <a href="/admin/discounts" class="btn btn-secondary">Hủy</a>
  </form>
</div>

<style>
/* --- Container tổng thể --- */
.container.admin-discount-add {
    background-color: #fff;
    padding: 25px 30px;
    border-radius: 10px;
    box-shadow: 0 2px 12px rgba(0,0,0,0.05);
    font-family: "Segoe UI", Roboto, sans-serif;
    color: #333;
    max-width: 600px;
    margin-top: 30px;
}

/* Tiêu đề */
.admin-discount-add h2 {
    font-weight: 600;
    color: #222;
    text-align: center;
}

/* Form control */
.admin-discount-add .form-control {
    border-radius: 8px;
    border: 1px solid #ccc;
    padding: 10px;
    transition: all 0.2s ease;
}

.admin-discount-add .form-control:focus {
    outline: none;
    border-color: #007bff;
    box-shadow: 0 0 5px rgba(0,123,255,0.2);
}

/* Label */
.admin-discount-add label {
    font-weight: 500;
    margin-bottom: 5px;
    display: block;
}

/* Checkbox custom */
.admin-discount-add .form-check {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 18px;
}

.admin-discount-add input[type="checkbox"] {
    width: 18px;
    height: 18px;
    accent-color: #28a745; /* màu xanh khi checked */
    cursor: pointer;
    transition: all 0.2s ease;
}

.admin-discount-add input[type="checkbox"]:hover {
    transform: scale(1.1);
}

/* Buttons */
.admin-discount-add .btn {
    border-radius: 8px;
    padding: 8px 16px;
    transition: all 0.2s ease;
    font-weight: 500;
}

/* Hover button */
.admin-discount-add .btn-success:hover {
    background-color: #218838;
    border-color: #1e7e34;
    color: #fff;
}

.admin-discount-add .btn-secondary:hover {
    background-color: #5a5a5a;
    color: #fff;
}

/* Khoảng cách giữa các input */
.admin-discount-add .mb-3 {
    margin-bottom: 18px;
}

/* Responsive nhỏ */
@media (max-width: 768px) {
    .container.admin-discount-add {
        padding: 20px 15px;
    }

    .admin-discount-add button {
        width: 100%;
        margin-bottom: 10px;
    }
}

</style>
